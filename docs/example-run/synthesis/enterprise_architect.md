Objective: Decide the target architecture, model-selection criteria, and integration blueprint the delivery team builds against — and state plainly that these decisions cannot be finalized until Legal confirms EU AI Act classification and until the human-in-the-loop map, including who reviews and who overrides, is actually drawn.

Body: "Scale the copilot" is three bundled decisions, and classification plus HITL mapping are gates, not parallel tracks. I cannot lock the model layer or audit design without them.

**Target architecture:** ingestion/ETL per source system → retrieval layer (vector store over claims/policy/member docs, scoped per function) → model gateway (provider-agnostic interface, not a direct SDK call) → orchestration/agent layer → HITL checkpoint service → audit/logging store (every prompt, retrieval, output, override, timestamped, retained). Claims, member services, and compliance get separate retrieval namespaces and access scopes.

**HITL checkpoint — named, not sketched:** low-confidence or high-stakes outputs (denial recommendations, coverage determinations) route to a named reviewer role, not a generic queue. [ASSUMPTION: this role is a senior claims adjuster or compliance lead with authority to reverse the model's output — basis: pilot design intent, unconfirmed]. The reviewer's override is logged with reviewer ID, timestamp, and rationale, feeding back into the accuracy-target evaluation. **This is currently undecided at the org level** — no one has confirmed which role holds override authority, whether they have time allocated for it, or whether overriding carries professional risk for them. That decision belongs to the steering committee and line managers together, not to architecture; I need their answer before the checkpoint service can be built, not just designed.

**Trust boundaries:** PHI stays inside the approved boundary; the model gateway is the only component permitted external calls, with redaction/tokenization before any payload leaves. The member-services PHI flow has not gone to Security — it should, before commitment.

**Failure behaviour:** if the gateway or retrieval layer is unavailable, fall back to the pre-copilot manual queue, not silent degrade to raw model output. Must be tested, not assumed.

**Model selection — criteria, not preference:** latency budget [ASSUMPTION: <3s, needs validation with Process Excellence], context window for policy documents, task-specific accuracy against a fixed target (none exists per function — this blocks final selection), data residency (EU healthcare data likely requires in-region hosting, pending Annex III classification), cost per claim, operational maturity to run self-hosted inference. **Rejected:** single frontier model via direct vendor SDK — cheapest to build, no exit if pricing or Act obligations change. **Provisional direction:** gateway abstraction over 1–2 providers, decided after the accuracy-target spike.

**One-way doors:** (1) PHI residence/provider — exit is data migration plus re-contracting, 3–6 months; (2) vector store/retrieval schema — cheap to change now, expensive after three functions are live; (3) fine-tuning on provider data — data cannot be fully retracted; no fine-tuning until Data Readiness and Legal sign off.

**Cost model:** [ASSUMPTION: 50k claims/month, 2k tokens avg context, 3 retries/claim — basis: pilot volume extrapolated, unvalidated]. Needs real throughput numbers before it enters a business case.

Citations: https://artificialintelligenceact.eu/article/14/ (Article 14, human oversight — drives audit/logging design regardless of final classification)

Risks: (1) Line managers are actively resisting scale-up — this is an organizational trust and workload problem architecture cannot fix; it belongs to the steering committee and change lead, and go-live should not proceed while it's unresolved, because an unresisted rollout with no adopted HITL role is a control that exists on paper only. (2) Building the gateway or logging schema before classification is confirmed risks rework if Annex III mandates specific record-keeping. (3) Poor claims data quality feeding retrieval has no fallback except full manual queue, and that queue's capacity at 2,000-person scale is unsized. (4) No accuracy target exists for any function — model selection is a preference dressed as a decision until it does. (5) Direct-SDK coupling, if adopted under time pressure, becomes an undiscussable one-way door in three years.

Next Steps: (1) Legal confirms Annex III status this week; audit/logging schema locks to Article 14 the day it lands. (2) Steering committee and line managers jointly name the HITL reviewer role, its override authority, and time allocation — by next steering meeting, owned by the change lead, not architecture. (3) Process Excellence and Data Readiness set a numeric accuracy target per function; no model selection without it. (4) Operations sizes manual-queue capacity at full 2,000-person scale, owner: Ops lead, due before go-live decision. (5) Spike the gateway against two providers on real claims-adjudication data. (6) Security reviews the PHI boundary for member services. (7) Delivery lead reports the two behind-schedule workstreams and revised dates directly to the steering committee — schedule risk is a program-management concern, not this seat's, but it needs an explicit owner named before go-live is scheduled.